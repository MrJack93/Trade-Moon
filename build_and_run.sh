#!/bin/bash
# Указывает, что скрипт должен выполняться с использованием командной оболочки Bash.

# =============================================================================
# ОПИСАНИЕ СКРИПТА
# =============================================================================
#
# Этот скрипт автоматизирует полный цикл пересборки и перезапуска
# Docker-контейнера приложения. Он выполняет следующие шаги:
# 1. Останавливает и удаляет существующий контейнер.
# 2. Очищает неиспользуемые Docker-ресурсы для освобождения места.
# 3. Собирает новый Docker-образ с нуля (без использования кэша).
# 4. Запускает новый контейнер в фоновом режиме.
# 5. Кратковременно выводит логи запущенного контейнера для быстрой проверки.
#
# =============================================================================


# --- Переменные конфигурации ---
# Имя контейнера, как оно определено в docker-compose.yml.
CONTAINER_NAME="tradex_container"
# Путь к файлу docker-compose, который управляет сборкой и запуском.
DOCKER_COMPOSE_FILE="docker-compose.yml"

# --- Функция для логирования ---
# Определяет функцию для вывода сообщений с временной меткой и префиксом [INFO].
# Это улучшает читаемость вывода скрипта.
log_message() {
    local MESSAGE="$1" # Принимает сообщение в качестве первого аргумента.
    echo "$(date '+%Y-%m-%d %H:%M:%S') [INFO] $MESSAGE" # Выводит отформатированное сообщение.
}

# --- Шаг 1: Остановка и удаление старого контейнера ---
log_message "🚀 Остановка и удаление старого контейнера..."
# Команда `docker-compose down` останавливает и удаляет контейнеры, сети и тома,
# определенные в указанном docker-compose файле.
# --remove-orphans: Удаляет контейнеры для сервисов, которых больше нет в файле docker-compose.
# `|| true`: Гарантирует, что скрипт не прервется с ошибкой, если контейнеров для остановки не существует.
docker-compose -f "$DOCKER_COMPOSE_FILE" down --remove-orphans || true

# --- Шаг 2: Очистка Docker-ресурсов ---
log_message "🧹 Очистка неиспользуемых Docker-ресурсов..."
# `docker system prune` удаляет все неиспользуемые данные Docker:
# - Остановленные контейнеры
# - Неиспользуемые сети
# - "Висячие" (dangling) образы и кэш сборки
# -f: Пропускает запрос на подтверждение.
# `> /dev/null 2>&1`: Перенаправляет стандартный вывод и вывод ошибок в "никуда", чтобы не засорять лог.
# `|| true`: Предотвращает остановку скрипта в случае возникновения ошибки при очистке.
docker system prune -f > /dev/null 2>&1 || true

# --- Шаг 3: Сборка нового Docker-образа ---
log_message "🔨 Сборка нового Docker-образа без кэша..."
# `docker-compose build` собирает образ для сервисов, определенных в docker-compose файле.
# --no-cache: Указывает Docker не использовать кэш предыдущих сборок. Это гарантирует,
# что все слои образа будут созданы заново с последними версиями файлов и зависимостей.
docker-compose -f "$DOCKER_COMPOSE_FILE" build --no-cache

# --- Шаг 4: Запуск нового контейнера ---
log_message "🚢 Запуск нового контейнера..."
# `docker-compose up` создает и запускает контейнеры.
# -d (detach): Запускает контейнеры в фоновом режиме и выводит только их имена.
docker-compose -f "$DOCKER_COMPOSE_FILE" up -d

# --- Шаг 5: Отображение логов ---
log_message "📜 Отображение логов в течение 10 секунд..."
# `timeout 10` ограничивает выполнение следующей команды 10 секундами.
# `docker-compose logs -f` отслеживает и выводит логи запущенных контейнеров в реальном времени.
# Это позволяет быстро проверить, что приложение запустилось без критических ошибок.
timeout 10 docker-compose -f "$DOCKER_COMPOSE_FILE" logs -f

# --- Завершение ---
log_message "✅ Процесс сборки успешно завершен!"
